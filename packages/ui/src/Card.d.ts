import type { CSSProperties, ReactNode } from "react";
export interface CardProps {
    title?: string;
    children: ReactNode;
    style?: CSSProperties;
    className?: string;
}
export declare function Card({ title, children, style, className }: CardProps): JSX.Element;
//# sourceMappingURL=Card.d.ts.map